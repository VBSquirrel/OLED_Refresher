<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <UseWindowsForms>true</UseWindowsForms>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>

    <AssemblyName>OledRefresher</AssemblyName>
    <RootNamespace>OledRefresher</RootNamespace>
    <Product>OLED Refresher</Product>
    <Company>OLED Refresher</Company>
    <Authors>Chris Scott</Authors>
    <!-- Local/dev default. Releases are versioned from the git tag: CI passes
         -p:Version=<tag>, and FileVersion/AssemblyVersion/InformationalVersion
         are derived from it automatically. -->
    <Version>1.0.0</Version>
    <Description>Periodic black-screen pixel refresh to reduce OLED burn-in during long gaming sessions.</Description>
    <NeutralLanguage>en</NeutralLanguage>

    <!-- Allow restore/build of this Windows-only app on non-Windows CI agents. -->
    <EnableWindowsTargeting>true</EnableWindowsTargeting>
    <SatelliteResourceLanguages>en</SatelliteResourceLanguages>
  </PropertyGroup>

</Project>
